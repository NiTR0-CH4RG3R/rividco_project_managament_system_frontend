import styled from '@emotion/styled'
import { IconButton } from '@mui/material'

const TopBarIconButton = styled(IconButton)(({ theme }) => ({
  //style form TopBarIconButton here
}))

export default TopBarIconButton

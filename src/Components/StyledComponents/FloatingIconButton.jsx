import styled from '@emotion/styled'
import { IconButton } from '@mui/material'

const FloatingIconButton = styled(IconButton)(({ theme }) => ({
  //style form Floating Icon Button  here
}))

export default FloatingIconButton

import styled from '@emotion/styled'
import { ListItemButton } from '@mui/material'

const NavSubItemListItemButton = styled(ListItemButton)(({ theme }) => ({
  //style form Nav Sub Item ListItemButton here
}))

export default NavSubItemListItemButton
